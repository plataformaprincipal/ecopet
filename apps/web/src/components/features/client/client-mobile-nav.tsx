"use client";

import { PrimaryBottomNav } from "@/components/shared/navigation/primary-bottom-nav";

/** Navegação móvel do cliente (/cliente) — exatamente 5 itens principais. */
export function ClientMobileNav() {
  return <PrimaryBottomNav context="clientPt" />;
}
