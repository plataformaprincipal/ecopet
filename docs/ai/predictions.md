# Previsões explicáveis

Motor heurístico (não ML opaco): frequência, recência, estoque.

Cada previsão inclui: objetivo, fontes, período, score, confiança, explicação, limitações, validade.

Persistência: `AIRecommendation` + auditoria.

API: `POST /api/ai/predictions`

Flag: `AI_FLAG_PREDICTIONS`
