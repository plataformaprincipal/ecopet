# Métricas

Emitidas como eventos estruturados (`event: metric`) via `trackMetric`.

Extração no Better Stack: criar métricas a partir de campos `metricName` / `metricValue` no painel (passo manual).

Sem `userId` em labels. Sem valores financeiros sensíveis.
