# Prisma / Supabase — ver [integrations.md](./integrations.md)

Helper: `observePrismaOperation(model, operation, fn)` — sem SQL/params.  
Não há extensão Prisma global automática (evita volume e PII).
