# Testes

```bash
npm run test:observability -w @ecopet/web
```

Cobre: redaction, circular, correlation, config/health sem token, classificação, fingerprint.

Integração real Better Stack: botão admin ou `POST /api/internal/observability/health` com `{ "confirm": true }`.
